moirai.to {
    encode zstd gzip
    request_body {
        max_size 33MB
    }
    reverse_proxy 127.0.0.1:8080
    # Deliberately omit access logs: capability URLs must not enter logs.
}
www.moirai.to {
    redir https://moirai.to{uri} permanent
}
